let a1 : number[] = [1]; // float or int
let b1 : string[] = ["Daniel"] // string
let c1 : boolean[] = [true] // bool
console.log(a1, b1, c1);

let series = [1, 2, 3];
let doubleIt = series.map(e => e * 2);
console.log(doubleIt);
